import { IActivity } from './activity.model';

export const paris = {
	id: 'paris',
	name: 'paris',
} as IActivity;
export const marseille = {
	id: 'marseille',
	name: 'marseille',
} as IActivity;
